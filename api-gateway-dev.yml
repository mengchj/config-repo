zuul:
  routes:
    #myProduct/list 路由到 product/list
    myProduct:
      path: /myProduct/**
      serviceId: product
      #cookies等有效
      sensitiveHeaders:
#简洁写法
#    product: /myProduct/**

    #排除指定路由
  ignored-patterns:
    - /**/product/listForOrder
management:
  security:
    enabled: false
